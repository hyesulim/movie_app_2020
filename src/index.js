import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));
// react application can render ONE component at a time
// so everything has to be inside of App